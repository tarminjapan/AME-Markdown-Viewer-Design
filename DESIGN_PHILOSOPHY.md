# 「伝わりやすいデザイン」テーマ理念

## 理念サマリー

本リポジトリのCSSテーマは、**「伝わりやすいデザイン」** を理念とする。

参照:
[伝わるデザイン — 情報をデザインする](https://tsutawarudesign.com/tsutaeru.html)

情報を正確かつ効率的に伝えるために、
以下の 3 つの役割を三位一体として成立させる。

| 役割             | 概要                                         |
| ---------------- | -------------------------------------------- |
| **読みやすさ**   | テキスト情報が抵抗なく読めること             |
| **見やすさ**     | 視覚要素が整理され、直感的に理解できること   |
| **見栄えのよさ** | 統一感のある美しいレイアウトで関心を引くこと |

そして、すべての人に配慮する
**バリアフリー（ユニバーサルデザイン）** を基盤として据える。

> デザインには、情報を整理して理解しやすい形にすることで、
> 受け手にストレスを与えず、正確かつ効率的に情報が伝わるという
> 役割がある。— 伝わるデザイン

---

## フォント設定

本テーマで使用するフォントファミリを以下の優先順位で定義する。
Webフォント（Google Fonts）を優先し、
最後に **generic font** を指定して互換性を確保する。

### セリフ（Serif）

本文の長文読みに適したフォント。

- Noto Serif
- Noto Serif JP
- serif

### サンセリフ（Sans-serif）

見出し・UI要素・本文に使用するフォント。

- Noto Sans
- Noto Sans JP
- sans-serif

### モノスペース（Monospace）

コード・等幅要素に使用するフォント。

- Noto Sans Mono
- BIZ UDGothic
- monospace

---

## 1. 読みやすさ (Readability)

テキストは情報伝達の主媒体である。
フォント、行間、文字サイズ、余白のすべてが適切に設定されて初めて、
読み手はテキストの「内容」に集中できる。
読みやすさは、最も基本的かつ最も重要なデザイン要素である。

### 読みやすさのチェック基準

| ID   | 基準                                                        | 判定方法            |
| ---- | ----------------------------------------------------------- | ------------------- |
| R-01 | `line-height` は **1.6 以上** であること                    | CSS値の確認         |
| R-02 | 「フォント設定」のフォントのみ使用（generic font 末尾必須） | `@import` 等の確認  |
| R-03 | 見出しサイズに階層があること（h1 > h2 > ...）               | font-size の確認    |
| R-04 | 段落（`p`）の上下余白は **0.8em 以上**                      | margin 値の確認     |
| R-05 | リストの左余白は **1.2em 以上**                             | padding-left の確認 |
| R-06 | `strong` を太字または色変更で区別                           | プロパティ値の確認  |
| R-07 | `letter-spacing` を適切に設定                               | プロパティ値の確認  |
| R-08 | `overflow-wrap: anywhere` を設定                            | プロパティ値の確認  |

---

## 2. 見やすさ (Visibility)

情報の視覚的構造が整理されていれば、
読み手は迷わず内容を理解できる。
コントラスト、余白、レイアウト、アクセントカラーの用法が、
情報の階層と関係性を視覚的に表現する。

### 見やすさのチェック基準

| ID   | 基準                                                 | 判定方法           |
| ---- | ---------------------------------------------------- | ------------------ |
| V-01 | 本文と背景のコントラスト比 **4.5:1 以上**（WCAG AA） | 色の輝度計算       |
| V-02 | 大見出しと背景のコントラスト比 **3:1 以上**          | 色の輝度計算       |
| V-03 | アクセントカラーは **1色基軸** + 派生色で統一        | CSS変数の確認      |
| V-04 | テーブルヘッダー（`th`）が背景色で区別               | background の確認  |
| V-05 | テーブルにストライプが設定されていること             | nth-child の確認   |
| V-06 | テーブル行にホバー効果があること                     | tr:hover の確認    |
| V-07 | 画像に `border` または `box-shadow`                  | プロパティ値の確認 |
| V-08 | コードブロックに境界線またはシャドウ                 | プロパティ値の確認 |
| V-09 | ブロック要素間の余白に一貫性があること               | margin を俯瞰      |

---

## 3. 見栄えのよさ (Attractiveness)

美しく統一された資料は、受け手の関心を引きつけ、
情報伝達の効果を高める。
デザイントークンによる一貫性、視覚的な階層、
細部まで行き届いた装飾がプロフェッショナルな印象を与える。

### 見栄えのよさのチェック基準

| ID   | 基準                                              | 判定方法                 |
| ---- | ------------------------------------------------- | ------------------------ |
| A-01 | 色・間隔・影などを **CSS変数** で定義             | `--` 変数の確認          |
| A-02 | Light/Dark両モードでデザイントークンを定義        | `:root` 等の確認         |
| A-03 | 見出しの視覚的階層（サイズ・太さの段階的変化）    | 各見出しのプロパティ比較 |
| A-04 | `border-radius` の値に一貫性（原則0または統一値） | 全角丸値の確認           |
| A-05 | `box-shadow` に一貫性（トークン使用）             | シャドウ値の確認         |
| A-06 | リンクに `:hover` 状態のスタイルが定義済み        | a:hover の確認           |
| A-07 | `hr` が装飾的（デフォルトの二重線ではない）       | プロパティ値の確認       |
| A-08 | `blockquote` が背景色・左ボーダー・余白で区別     | プロパティ値の確認       |

---

## 4. バリアフリー (Universal Design)

情報を受け取る人には、視覚の能力や感覚に多様性が存在する。
色覚多様性や視覚過敏への配慮を欠いたデザインは、
正確な情報伝達を妨げる。
すべての人にとって見やすいデザインが、
多様性社会におけるコミュニケーションの基盤となる。

### バリアフリーのチェック基準

| ID   | 基準                                              | 判定方法                |
| ---- | ------------------------------------------------- | ----------------------- |
| U-01 | **ダークモード** に対応していること               | メディアクエリの確認    |
| U-02 | ダークモードでもコントラスト比 **4.5:1 以上**     | 輝度計算                |
| U-03 | **フォーカスリング**（`focus-visible`）が定義済み | `:focus-visible` の確認 |
| U-04 | 情報の区別を **色だけに依存しない**               | デザイン手法の俯瞰      |
| U-05 | **レスポンシブ対応** がされていること             | メディアクエリの確認    |
| U-06 | モバイルでも `line-height` 1.6以上・適切な余白    | モバイル用値の確認      |
| U-07 | リンク色と本文色の **コントラスト比 3:1 以上**    | 色の輝度計算            |
| U-08 | `::selection` のハイライトが定義済み              | ルールの確認            |

---

## リント無視の禁止

本リポジトリでは、**あらゆるリント無視を禁止** する。

- インラインの `stylelint-disable`、`markdownlint-disable`、
  `eslint-disable`、`prettier-ignore` 等のコメントは使用しない
- lint 設定ファイル（`.stylelintrc.json`、`eslint.config.js` 等）でルールを無効化
  （`null` 等）しない
- lint ルールに違反した場合は、**対象コードを修正** すること

### 禁止される設定例

```json
{
  "rules": {
    "font-family-no-missing-generic-family-keyword": null
  }
}
```

上記のようなルールの無効化は許可されない。

### 禁止されるコメント例

```css
/* stylelint-disable-next-line */ /* 禁止 */
/* prettier-ignore */ /* 禁止 */
```

```javascript
// eslint-disable-next-line no-unused-vars  // 禁止
/* eslint-disable */ // 禁止
// prettier-ignore                           // 禁止
```

---

## AI チェックプロトコル

### 実行タイミング

以下のいずれかの条件を満たした場合、
このドキュメントのチェック基準に照合すること。

- CSSファイル（`*.css`）を新規作成したとき
- 既存のCSSファイルを変更したとき
- ユーザーから「デザインチェック」または同等の指示を受けたとき

### チェック手順

1. 対象のCSSファイルを読み込む
2. 本ドキュメントの全チェック基準
   （R-01~R-08, V-01~V-09, A-01~A-08, U-01~U-08）を順に評価
3. 各基準について **適合（PASS）** または
   **不適合（FAIL）** を判定し、理由を併記する
4. 不適合項目がある場合は、修正の具体案を提示する
5. 結果を以下の形式で報告する

### 報告フォーマット

```markdown
## デザイン理念チェック結果

対象ファイル: <ファイル名>

### 読みやすさ (Readability)

| ID   | 基準              | 判定 | 備考    |
| ---- | ----------------- | ---- | ------- |
| R-01 | line-height ≥ 1.6 | PASS | 値: 1.8 |
| ...  | ...               | ...  | ...     |

### 見やすさ (Visibility)

（同形式）

### 見栄えのよさ (Attractiveness)

（同形式）

### バリアフリー (Universal Design)

（同形式）

### サマリー

- 適合: X / Y 項目
- 不適合項目の修正案:
  1. ...
```

### コントラスト比の計算方法

WCAG 2.0 に基づく相対輝度（relative luminance）を用いて計算する。

```text
1. sRGB → 線形: 各チャンネルを 0〜1 に正規化し、
   値 ≤ 0.04045 → 値 / 12.92
   値 > 0.04045 → ((値 + 0.055) / 1.055) ^ 2.4

2. 相対輝度 L = 0.2126 * R + 0.7152 * G + 0.0722 * B

3. コントラスト比 = (L1 + 0.05) / (L2 + 0.05)
   （L1: 明るい色の輝度, L2: 暗い色の輝度）
```
